% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prefer.R
\name{dragon_prefer}
\alias{dragon_prefer}
\title{Preference optimization with DPO or ORPO}
\usage{
dragon_prefer(
  dataset,
  model,
  method = c("dpo", "orpo"),
  beta = 0.1,
  lora = dragon_lora(),
  args = dragon_train_args(learning_rate = 5e-05, epochs = 2),
  hardware = dragon_hardware(),
  name = NULL,
  run_dir = NULL,
  runs_dir = dragon_runs_dir(),
  n_samples = 10,
  revision = NULL,
  trust_remote_code = FALSE,
  wait = FALSE
)
}
\arguments{
\item{dataset}{A dataset mapped with \code{\link[=dragon_map_pairs]{dragon_map_pairs()}}.}

\item{model}{A Hugging Face model id such as
\code{"HuggingFaceTB/SmolLM2-135M-Instruct"}, a local model directory, or a
finished \code{dragon_run} to continue from. In the last case the earlier
run's adapters are folded into the weights before this run adds its own,
so stages chain: fine-tune, then \code{\link[=dragon_prefer]{dragon_prefer()}}, and so on. See
\code{\link[=dragon_presets]{dragon_presets()}} for model ids.}

\item{method}{\code{"dpo"} or \code{"orpo"}.}

\item{beta}{Preference strength. Typical values are 0.05 to 0.5.}

\item{lora}{LoRA settings from \code{\link[=dragon_lora]{dragon_lora()}}.}

\item{args}{Training settings. The defaults use a lower learning rate than
\code{\link[=dragon_train]{dragon_train()}}, which preference methods need.}

\item{hardware}{Hardware settings from \code{\link[=dragon_hardware]{dragon_hardware()}}.}

\item{name}{Short label used in the run id. Defaults to the model name.}

\item{run_dir}{Exact directory to use. Defaults to a timestamped directory
under \code{runs_dir}.}

\item{runs_dir}{Parent directory for runs. See \code{\link[=dragon_runs_dir]{dragon_runs_dir()}}.}

\item{n_samples}{Number of held-out rows to generate sample replies for
at the end of training.}

\item{revision}{Model revision (branch, tag, or commit) on the Hub.}

\item{trust_remote_code}{Allow the model repository to run custom code.}

\item{wait}{Block until training finishes.}
}
\value{
A \code{dragon_run} object.
}
\description{
The second stage of post-training. Where \code{\link[=dragon_train]{dragon_train()}} teaches a model
what a good reply looks like, this teaches it which of two replies is
better, from a dataset mapped with \code{\link[=dragon_map_pairs]{dragon_map_pairs()}}.
}
\details{
Two methods are available:
\itemize{
\item \code{"dpo"}, Direct Preference Optimization. Pushes the model's implicit
reward for the chosen reply above the rejected one, relative to a
reference model. With LoRA the reference is the same model with the
adapter switched off, so it costs no extra memory.
\item \code{"orpo"}, Odds Ratio Preference Optimization. Combines the supervised
loss on the chosen reply with an odds-ratio penalty on the rejected one.
Needs no reference model and works from a base model that has not been
fine-tuned yet.
}

\code{beta} controls how hard the model is pushed: the KL strength for DPO,
the odds-ratio weight for ORPO. \code{0.1} is a sensible start for both.

Pass a finished run as \code{model} to continue from it. Its adapters are
folded into the weights before this stage adds its own, which is the
usual sequence: \code{\link[=dragon_train]{dragon_train()}} first, then \code{dragon_prefer()} on top.
}
\examples{
\dontrun{
sft <- dragon_dataset("tickets.csv") |>
  dragon_map(prompt = "question", response = "answer") |>
  dragon_train("Qwen/Qwen2.5-0.5B-Instruct", wait = TRUE)

dpo <- dragon_dataset("preferences.csv") |>
  dragon_map_pairs(prompt = "question", chosen = "better", rejected = "worse") |>
  dragon_prefer(sft, method = "dpo", beta = 0.1, wait = TRUE)

dragon_evaluate(dpo)
dragon_generate(dpo, "My thermostat keeps dropping off Wi-Fi.")
}
}
