% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{dragon_archive_run}
\alias{dragon_archive_run}
\alias{dragon_unarchive_run}
\alias{dragon_delete_run}
\title{Archive, restore, or delete a run}
\usage{
dragon_archive_run(run, runs_dir = dragon_runs_dir(), force = FALSE)

dragon_unarchive_run(id, runs_dir = dragon_runs_dir())

dragon_delete_run(run, runs_dir = dragon_runs_dir(), force = FALSE)
}
\arguments{
\item{run}{A \code{dragon_run}, run directory, or run id (with \code{runs_dir}).}

\item{runs_dir}{Where runs live. Needed only when \code{run}/\code{id} is a bare id.}

\item{force}{Archive or delete even if another run continues from this one.}

\item{id}{An archived run's id, for \code{\link[=dragon_unarchive_run]{dragon_unarchive_run()}}.}
}
\value{
The run id, invisibly.
}
\description{
Archiving moves a run's directory under \verb{archived/} in \code{runs_dir}. It
disappears from \code{\link[=dragon_runs]{dragon_runs()}} and the app's Runs list, but every file
is kept; \code{\link[=dragon_unarchive_run]{dragon_unarchive_run()}} moves it back exactly as it was.
Deleting removes the run directory for good. Both refuse a run that is
queued or running (cancel it first) and a run that another run
continues from, unless \code{force = TRUE}.
}
\examples{
\dontrun{
dragon_archive_run(run)
dragon_archived_runs()
dragon_unarchive_run(run$id)
dragon_delete_run("20260101-000000-old-experiment")
}
}
