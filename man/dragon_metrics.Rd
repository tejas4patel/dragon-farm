% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{dragon_metrics}
\alias{dragon_metrics}
\alias{dragon_metric_regex}
\title{Task metrics for generated replies}
\usage{
dragon_metrics()

dragon_metric_regex(pattern, ignore_case = TRUE)
}
\arguments{
\item{pattern}{A regular expression the reply must match.}

\item{ignore_case}{Case-insensitive match.}
}
\value{
\code{dragon_metrics()}: a named list of metric functions.

\code{dragon_metric_regex()}: a metric function.
}
\description{
Deterministic checks that need no judge model. Each metric is a function
of three character vectors, \code{generated}, \code{reference}, and \code{prompt}, and
returns one number per row (0 or 1 for pass/fail metrics). Pass names
from this list, or your own functions, to \code{\link[=dragon_evaluate]{dragon_evaluate()}}.
}
\details{
\itemize{
\item \code{exact}: normalized exact match (case, whitespace, and trailing
punctuation ignored).
\item \code{contains}: the normalized reference appears inside the reply.
\item \code{token_f1}: token overlap F1 between reply and reference, the SQuAD
style partial-credit score.
\item \code{json_valid}: the reply parses as JSON (a fenced code block is unwrapped
first).
\item \code{numeric}: the last number in the reply equals the last number in the
reference.
\item \code{length_ratio}: characters in the reply divided by characters in the
reference. Useful for spotting rambling or truncation.
}

\code{dragon_metric_regex()} builds a metric that passes when the reply
matches a pattern, for format checks such as "starts with a ticket id".
}
\examples{
m <- dragon_metrics()
m$exact("Paris.", "paris", "Capital of France?")
m$token_f1("the cat sat on the mat", "a cat sat on a mat", "")
m$json_valid('```json\n{"a": 1}\n```', "", "")
}
