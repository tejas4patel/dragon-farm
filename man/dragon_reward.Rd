% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reinforce.R
\name{dragon_reward}
\alias{dragon_reward}
\title{Verifiable rewards for reinforcement learning}
\usage{
dragon_reward(
  type = c("exact", "contains", "numeric", "regex", "json", "length", "keyword",
    "custom"),
  weight = 1,
  name = NULL,
  pattern = NULL,
  case_sensitive = FALSE,
  keys = NULL,
  min_chars = NULL,
  max_chars = NULL,
  words = NULL,
  mode = c("any", "all"),
  file = NULL,
  fn = "reward"
)
}
\arguments{
\item{type}{Which reward.}

\item{weight}{Multiplier when rewards are summed.}

\item{name}{Label used in progress rows and evaluation. Defaults to the type.}

\item{pattern}{Regular expression, for \code{"regex"}.}

\item{case_sensitive}{Whether \code{pattern} is case-sensitive.}

\item{keys}{Required top-level keys, for \code{"json"}.}

\item{min_chars, max_chars}{Bounds, for \code{"length"}. Give at least one.}

\item{words}{Words to look for, for \code{"keyword"}.}

\item{mode}{\code{"any"} or \code{"all"} of \code{words}.}

\item{file}{Python file, for \code{"custom"}.}

\item{fn}{Name of the function inside \code{file}.}
}
\value{
A \code{dragon_reward} object.
}
\description{
A reward scores one completion between 0 and 1. \code{\link[=dragon_reinforce]{dragon_reinforce()}} takes
one or more, sums them by \code{weight}, and pushes the model towards higher
totals. These built-ins are verifiable: they check facts about the text
rather than asking a model's opinion, which is what makes reinforcement
learning work on small models.
}
\details{
\itemize{
\item \code{"exact"}: normalized exact match with the row's reference.
\item \code{"contains"}: the reference appears in the completion.
\item \code{"numeric"}: the last number in the completion equals the reference's.
\item \code{"regex"}: the completion matches \code{pattern}.
\item \code{"json"}: the completion is valid JSON, with \code{keys} present if given
(partial credit per key).
\item \code{"length"}: 1 within \code{min_chars} to \code{max_chars}, falling to 0 beyond.
\item \code{"keyword"}: any (or all) of \code{words} appear.
\item \code{"custom"}: a Python \code{file} defining \code{reward(prompt, completion, reference, row)} that returns a number. The file is copied into the run
so the run stays self-contained.
}
}
\examples{
dragon_reward("exact")
dragon_reward("regex", pattern = "^T-\\\\d{4}", weight = 2)
dragon_reward("length", max_chars = 400, weight = 0.5)
dragon_reward("json", keys = c("id", "status"))
}
