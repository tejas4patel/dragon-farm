% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reinforce.R
\name{dragon_reinforce}
\alias{dragon_reinforce}
\title{Reinforcement learning with verifiable rewards (GRPO)}
\usage{
dragon_reinforce(
  dataset,
  model,
  rewards,
  group_size = 4,
  beta = 0.04,
  temperature = 1,
  max_new_tokens = 128,
  lora = dragon_lora(),
  args = dragon_train_args(learning_rate = 1e-05, epochs = 1, batch_size = 4, grad_accum
    = 1, save_steps = 20),
  hardware = dragon_hardware(),
  name = NULL,
  run_dir = NULL,
  runs_dir = dragon_runs_dir(),
  n_samples = 10,
  revision = NULL,
  trust_remote_code = FALSE,
  wait = FALSE
)
}
\arguments{
\item{dataset}{A dataset mapped with \code{\link[=dragon_map_prompts]{dragon_map_prompts()}}.}

\item{model}{A Hugging Face model id such as
\code{"HuggingFaceTB/SmolLM2-135M-Instruct"}, a local model directory, or a
finished \code{dragon_run} to continue from. In the last case the earlier
run's adapters are folded into the weights before this run adds its own,
so stages chain: fine-tune, then \code{\link[=dragon_prefer]{dragon_prefer()}}, and so on. See
\code{\link[=dragon_presets]{dragon_presets()}} for model ids.}

\item{rewards}{A \code{\link[=dragon_reward]{dragon_reward()}} or a list of them.}

\item{group_size}{Completions sampled per prompt. 4 to 8 is typical; more
gives a better baseline at more cost per step.}

\item{beta}{Weight of the KL penalty towards the starting model. Higher
is more conservative.}

\item{temperature}{Sampling temperature for the completions. Must be
above zero so the group varies.}

\item{max_new_tokens}{Length cap for each sampled completion.}

\item{lora}{LoRA settings from \code{\link[=dragon_lora]{dragon_lora()}}.}

\item{args}{Training settings. \code{batch_size} is prompts per step (so
\code{batch_size * group_size} completions), \code{epochs} or \code{max_steps} set the
length, and \code{save_steps} the checkpoint interval. The defaults use a
small learning rate, which RL needs.}

\item{hardware}{Hardware settings from \code{\link[=dragon_hardware]{dragon_hardware()}}.}

\item{name}{Short label used in the run id. Defaults to the model name.}

\item{run_dir}{Exact directory to use. Defaults to a timestamped directory
under \code{runs_dir}.}

\item{runs_dir}{Parent directory for runs. See \code{\link[=dragon_runs_dir]{dragon_runs_dir()}}.}

\item{n_samples}{Number of held-out rows to generate sample replies for
at the end of training.}

\item{revision}{Model revision (branch, tag, or commit) on the Hub.}

\item{trust_remote_code}{Allow the model repository to run custom code.}

\item{wait}{Block until training finishes.}
}
\value{
A \code{dragon_run} object.
}
\description{
The third stage of post-training. For every prompt the model writes
several completions, each is scored by the rewards, and the model is
nudged towards the completions that beat their group's average. A KL
penalty against the model it started from keeps it from drifting. This is
Group Relative Policy Optimization, the method behind recent reasoning
models, in its plain on-policy form.
}
\details{
It works well when the reward is something you can check: a correct
number, valid JSON with the right keys, a required format, a length
budget, a test that passes. It works poorly as a substitute for
preference data on vague goals such as "be more helpful"; use
\code{\link[=dragon_prefer]{dragon_prefer()}} for those.

Pass a finished run as \code{model} to continue from it, which is the usual
order: \code{\link[=dragon_train]{dragon_train()}}, optionally \code{\link[=dragon_prefer]{dragon_prefer()}}, then this.
}
\examples{
\dontrun{
math <- dragon_dataset("arithmetic.csv") |>
  dragon_map_prompts(prompt = "question", reference = "answer")
rl <- dragon_reinforce(
  math, sft,
  rewards = list(dragon_reward("numeric"), dragon_reward("length", max_chars = 300, weight = 0.2)),
  group_size = 6, wait = TRUE
)
dragon_evaluate(rl)     # mean reward on held-out prompts, per reward
}
}
