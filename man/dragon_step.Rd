% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{dragon_step}
\alias{dragon_step}
\alias{dragon_step_train}
\alias{dragon_step_prefer}
\alias{dragon_step_synthesize_pairs}
\alias{dragon_step_reinforce}
\alias{dragon_step_judge}
\alias{dragon_step_evaluate}
\title{Steps of a post-training pipeline}
\usage{
dragon_step_train(dataset, lora = dragon_lora(), args = NULL, n_samples = 10)

dragon_step_prefer(
  dataset = NULL,
  method = c("dpo", "orpo"),
  beta = 0.1,
  lora = dragon_lora(),
  args = NULL,
  n_samples = 10
)

dragon_step_synthesize_pairs(
  prompts = "train",
  n = 100,
  judge = NULL,
  n_samples_per_prompt = 4,
  min_gap = 2,
  rubric = NULL,
  temperature = 0.8,
  max_new_tokens = 256
)

dragon_step_reinforce(
  dataset,
  rewards,
  group_size = 4,
  beta = 0.04,
  temperature = 1,
  max_new_tokens = 128,
  lora = dragon_lora(),
  args = NULL,
  n_samples = 10
)

dragon_step_judge(against = "base", judge = NULL, n = 20, rubric = NULL)

dragon_step_evaluate(metrics = TRUE)
}
\arguments{
\item{dataset}{A mapped dataset for the stage. \code{dragon_step_prefer()}
may leave it \code{NULL} to use the pairs made by the preceding
\code{dragon_step_synthesize_pairs()}.}

\item{lora, args, n_samples}{As in \code{\link[=dragon_train]{dragon_train()}}. \code{args = NULL} uses the
stage's own defaults.}

\item{method, beta}{As in \code{\link[=dragon_prefer]{dragon_prefer()}}.}

\item{prompts}{\code{"train"} or \code{"eval"} to take prompts from the current run's
data, or a character vector or mapped dataset.}

\item{n}{How many prompts to use.}

\item{judge, rubric}{As in \code{\link[=dragon_judge]{dragon_judge()}}.}

\item{n_samples_per_prompt, min_gap, temperature, max_new_tokens}{As in
\code{\link[=dragon_synthesize_pairs]{dragon_synthesize_pairs()}}.}

\item{rewards, group_size}{As in \code{\link[=dragon_reinforce]{dragon_reinforce()}}.}

\item{against}{As in \code{\link[=dragon_judge]{dragon_judge()}}.}

\item{metrics}{As in \code{\link[=dragon_evaluate]{dragon_evaluate()}}.}
}
\value{
A \code{dragon_step} object.
}
\description{
Each step becomes one stage of \code{\link[=dragon_pipeline]{dragon_pipeline()}}. Stages chain: a
training step's run is the starting point of the next training step, a
synthesis step's pairs feed the next preference step, and judge and
evaluate steps measure the most recent run.
}
\examples{
\dontrun{
steps <- list(
  dragon_step_train(tickets),
  dragon_step_synthesize_pairs(prompts = "train", n = 150, judge = dragon_judge_anthropic()),
  dragon_step_prefer(),
  dragon_step_judge(against = "base", judge = dragon_judge_anthropic()),
  dragon_step_evaluate(metrics = c("token_f1", "length_ratio"))
)
p <- dragon_pipeline("Qwen/Qwen2.5-0.5B-Instruct", steps)
dragon_compare(p)
}
}
