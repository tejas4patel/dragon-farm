% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{dragon_pipeline}
\alias{dragon_pipeline}
\title{Run several post-training stages as one pipeline}
\usage{
dragon_pipeline(
  model,
  steps,
  runs_dir = dragon_runs_dir(),
  name = "pipeline",
  background = FALSE
)
}
\arguments{
\item{model}{Where the first training stage starts: a model id, a model
directory, or a finished \code{dragon_run}.}

\item{steps}{A list of \link{dragon_step} objects.}

\item{runs_dir}{Where runs and the pipeline record are written.}

\item{name}{Label used in the pipeline id.}

\item{background}{Run in a separate R process.}
}
\value{
A \code{dragon_pipeline} object. In the foreground it holds the runs
and every step's summary; in the background it is a handle whose
progress \code{\link[=dragon_pipeline_status]{dragon_pipeline_status()}} reads.
}
\description{
Executes the steps in order, threading the result of each into the next:
every training stage starts from the previous stage's run, synthesized
pairs feed the next preference stage, and judge and evaluate steps score
the latest run. Progress is written to \verb{pipelines/<id>.json} under
\code{runs_dir} after every step, so a pipeline can be watched from the app or
another session with \code{\link[=dragon_pipeline_status]{dragon_pipeline_status()}}.
}
\details{
With \code{background = TRUE} the pipeline runs in a separate R process and the
call returns at once. Steps must then be serializable: judges and
teachers made with \code{\link[=dragon_llm_anthropic]{dragon_llm_anthropic()}} or plain functions are fine,
\code{ellmer} chat objects are not.
}
\examples{
\dontrun{
p <- dragon_pipeline("Qwen/Qwen2.5-0.5B-Instruct", list(
  dragon_step_train(tickets),
  dragon_step_synthesize_pairs(judge = dragon_judge_anthropic(model = "claude-sonnet-5")),
  dragon_step_prefer(),
  dragon_step_judge(judge = dragon_judge_anthropic())
), background = TRUE)
dragon_pipeline_status(p)
}
}
