% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairs.R
\name{dragon_map_pairs}
\alias{dragon_map_pairs}
\title{Map columns for preference optimization}
\usage{
dragon_map_pairs(dataset, prompt, chosen, rejected, system = NULL)
}
\arguments{
\item{dataset}{A \code{dragon_dataset}.}

\item{prompt}{Column name or template for the user turn.}

\item{chosen}{Column name or template for the preferred reply.}

\item{rejected}{Column name or template for the reply to move away from.}

\item{system}{Optional column name or template for the system prompt. A
template with no braces and no matching column is used as a constant
system prompt for every row.}
}
\value{
The dataset with the mapping attached.
}
\description{
For \code{\link[=dragon_prefer]{dragon_prefer()}}. Each row holds one prompt and two candidate replies:
the one you prefer and the one you want the model to move away from. As in
\code{\link[=dragon_map]{dragon_map()}}, each argument is a column name or a \code{\link[glue:glue]{glue::glue()}} template
combining several columns.
}
\examples{
df <- data.frame(
  q = c("What is 2 + 2?", "Capital of France?"),
  good = c("4", "Paris"),
  bad = c("5", "Lyon")
)
ds <- dragon_map_pairs(dragon_dataset(df), prompt = "q", chosen = "good", rejected = "bad")
dragon_preview(ds, n = 1)
}
