% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{dragon_train_args}
\alias{dragon_train_args}
\title{Training settings}
\usage{
dragon_train_args(
  epochs = 3,
  learning_rate = 2e-04,
  batch_size = 4,
  grad_accum = 4,
  max_seq_len = 1024,
  max_steps = NULL,
  warmup_ratio = 0.03,
  weight_decay = 0,
  logging_steps = 5,
  save_steps = 100,
  gradient_checkpointing = FALSE,
  seed = 42,
  ...
)
}
\arguments{
\item{epochs}{Passes over the training data. Ignored when \code{max_steps} is set.}

\item{learning_rate}{Peak learning rate. \code{2e-4} is a good LoRA default.}

\item{batch_size}{Examples per device per step.}

\item{grad_accum}{Steps to accumulate before an optimizer update. Effective
batch size is \code{batch_size * grad_accum}.}

\item{max_seq_len}{Maximum tokens per example. Longer examples are truncated.}

\item{max_steps}{Stop after this many optimizer steps. \code{NULL} trains for \code{epochs}.}

\item{warmup_ratio}{Fraction of steps used to warm up the learning rate.}

\item{weight_decay}{Weight decay for the optimizer.}

\item{logging_steps}{Steps between progress rows.}

\item{save_steps}{Steps between checkpoints.}

\item{gradient_checkpointing}{Trade compute for memory. Turn on if you run
out of GPU memory.}

\item{seed}{Random seed.}

\item{...}{Extra named arguments passed straight to \code{TrainingArguments}.}
}
\value{
A \code{dragon_train_args} object.
}
\description{
Training settings
}
\examples{
dragon_train_args(epochs = 1, learning_rate = 1e-4)
}
