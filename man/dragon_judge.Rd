% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/judge.R
\name{dragon_judge}
\alias{dragon_judge}
\title{Judge a run's replies with a language model}
\usage{
dragon_judge(
  x,
  against = NULL,
  prompts = NULL,
  n = 20,
  judge = NULL,
  rubric = NULL,
  system = NULL,
  max_new_tokens = 256,
  seed = 42
)
}
\arguments{
\item{x}{A \code{dragon_run} or run directory whose replies are judged.}

\item{against}{What to compare with: \code{NULL} for scoring alone, \code{"base"}
for the model the run started from (its base model, or the run it
continued), or another run, model id, or model directory.}

\item{prompts}{Prompts to use. Defaults to \code{n} prompts from the run's
held-out rows.}

\item{n}{How many held-out prompts to use when \code{prompts} is \code{NULL}.}

\item{judge}{The judge: a function taking a character vector of prompts
and returning a character vector of replies, an \code{ellmer} chat object, or
a Hugging Face model id or local model directory to use as a local judge.
See \code{\link[=dragon_judge_anthropic]{dragon_judge_anthropic()}} for the Claude API.}

\item{rubric}{What the judge should value. A sentence or two; a sensible
default covers correctness, helpfulness, and following instructions.}

\item{system}{System prompt used when generating the replies being judged.}

\item{max_new_tokens}{Length cap for the generated replies.}

\item{seed}{Seed for sampling the held-out prompts.}
}
\value{
A \code{dragon_judgement} object: a list with \code{mode}, \code{summary}, and
\code{details} (one row per prompt).
}
\description{
Two modes. With \code{against = NULL}, each reply is scored from 1 to 10 against
a rubric, using the held-out reference answer as ground truth when there
is one. With \code{against} set, the run's replies are compared pairwise with
another model's replies to the same prompts, and the judge picks a winner.
Pairwise judging asks each question twice with the two replies swapped, so
a judge that favours whichever answer comes first cannot bias the result.
}
\details{
Prompts default to the run's held-out set, so scores are comparable across
runs that share a dataset. Results are written to \code{judge.json} in the run
directory and the summary is recorded in the run's status, where
\code{\link[=dragon_compare]{dragon_compare()}} picks it up.
}
\examples{
\dontrun{
# Did preference optimization help? Compare the DPO run with the SFT run it started from.
j <- dragon_judge(dpo, against = "base", judge = dragon_judge_anthropic())
j$summary

# Absolute scores with a task-specific rubric.
dragon_judge(sft, rubric = "Reward replies that give concrete next steps and stay under 120 words.",
             judge = dragon_judge_anthropic(model = "claude-sonnet-5"))

# A local judge: any model dragon_generate() can load.
dragon_judge(sft, against = "base", judge = "Qwen/Qwen2.5-1.5B-Instruct")
}
}
