% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remote.R
\name{dragon_bundle}
\alias{dragon_bundle}
\title{Package a run for a cloud GPU}
\usage{
dragon_bundle(
  dataset,
  model,
  lora = dragon_lora(),
  args = dragon_train_args(),
  name = NULL,
  run_dir = NULL,
  runs_dir = dragon_runs_dir(),
  n_samples = 10,
  revision = NULL,
  trust_remote_code = FALSE
)
}
\arguments{
\item{dataset}{A mapped \code{dragon_dataset} (see \code{\link[=dragon_map]{dragon_map()}}).}

\item{model}{A Hugging Face model id such as
\code{"HuggingFaceTB/SmolLM2-135M-Instruct"}, or a local model directory.
See \code{\link[=dragon_presets]{dragon_presets()}}.}

\item{lora}{LoRA settings from \code{\link[=dragon_lora]{dragon_lora()}}.}

\item{args}{Training settings from \code{\link[=dragon_train_args]{dragon_train_args()}}.}

\item{name}{Short label used in the run id. Defaults to the model name.}

\item{run_dir}{Exact directory to use. Defaults to a timestamped directory
under \code{runs_dir}.}

\item{runs_dir}{Parent directory for runs. See \code{\link[=dragon_runs_dir]{dragon_runs_dir()}}.}

\item{n_samples}{Number of held-out rows to generate sample replies for
at the end of training.}

\item{revision}{Model revision (branch, tag, or commit) on the Hub.}

\item{trust_remote_code}{Allow the model repository to run custom code.}
}
\value{
A \code{dragon_run} whose state is \code{"bundled"}.
}
\description{
Writes a run directory exactly as \code{\link[=dragon_train]{dragon_train()}} would, but instead of
launching the trainer it zips everything a GPU machine needs: the data in
chat format, the configuration, the trainer's Python code, and a notebook
that runs it. Nothing is trained locally and Python is not needed.
}
\details{
Continue with \code{\link[=dragon_remote]{dragon_remote()}} to open a provider and see the steps, and
\code{\link[=dragon_import]{dragon_import()}} to bring the results back into this run directory.
}
\examples{
\dontrun{
run <- dragon_dataset(dragon_example_data()) |>
  dragon_map(prompt = "{subject}\n\n{body}", response = "reply") |>
  dragon_bundle("Qwen/Qwen2.5-0.5B-Instruct")
dragon_remote(run, "colab")
# ... train in the browser, download the results zip ...
dragon_import(run, "~/Downloads/dragonfarm-results-<run id>.zip")
dragon_generate(run, "My thermostat keeps dropping off Wi-Fi.")
}
}
