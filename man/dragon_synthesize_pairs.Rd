% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth.R
\name{dragon_synthesize_pairs}
\alias{dragon_synthesize_pairs}
\title{Build preference pairs from a model's own samples}
\usage{
dragon_synthesize_pairs(
  prompts,
  student,
  judge = NULL,
  teacher = NULL,
  n_samples = 4,
  min_gap = 2,
  rubric = NULL,
  system = NULL,
  temperature = 0.8,
  max_new_tokens = 256,
  file = NULL,
  runs_dir = dragon_runs_dir(),
  name = "preferences"
)
}
\arguments{
\item{prompts}{A character vector of prompts, or a mapped \code{dragon_dataset}
whose prompt template is rendered for every row. See \code{\link[=dragon_prompts]{dragon_prompts()}}
for prompts from an existing run, which is how distillation from "the
model's own prompts" is done: pass \code{dragon_prompts(run, "train")}.}

\item{student}{The model whose replies are being improved: normally the
\code{dragon_run} you will continue from. Also a model id, ellmer chat, or
function.}

\item{judge}{Scores the student's samples. See \code{\link[=dragon_judge]{dragon_judge()}} for what
is accepted. Required unless \code{teacher} is given.}

\item{teacher}{Writes the chosen reply instead of judging. See
\code{\link[=dragon_synthesize]{dragon_synthesize()}}.}

\item{n_samples}{Samples per prompt in judge mode. At least 2.}

\item{min_gap}{Minimum score difference between chosen and rejected in
judge mode. Pairs below it are dropped.}

\item{rubric}{What the judge should value. See \code{\link[=dragon_judge]{dragon_judge()}}.}

\item{system}{System prompt for the teacher. Also stored with every row
so the student trains with the same instruction.}

\item{temperature}{Sampling temperature for the student. Needs to be
above zero in judge mode, or every sample is the same.}

\item{max_new_tokens}{Reply length cap for local teachers.}

\item{file}{Where to write the JSONL. Defaults to a timestamped file under
\verb{synth/} in \code{runs_dir}.}

\item{runs_dir}{Parent directory for the default \code{file}.}

\item{name}{Label used in the default file name.}
}
\value{
A \code{dragon_dataset} mapped as preference pairs. In judge mode the
file also records \code{chosen_score} and \code{rejected_score}.
}
\description{
Preference optimization needs, for each prompt, a better and a worse
reply. This function makes them without hand labelling, in one of two
ways:
}
\details{
\itemize{
\item With a \code{judge}: the student answers each prompt \code{n_samples} times at a
non-zero temperature, the judge scores every sample from 1 to 10, and
the best and worst become the chosen and rejected replies. Prompts whose
samples are too close (\code{min_gap}) or identical are dropped. This is the
loop behind RLAIF-style training: the model improves on its own outputs
under a judge's preferences.
\item With a \code{teacher}: the teacher's reply is chosen and the student's is
rejected. Cheap and effective when the teacher is clearly stronger.
}

The result is saved as JSONL and returned as a dataset mapped with
\code{\link[=dragon_map_pairs]{dragon_map_pairs()}}, ready for \code{\link[=dragon_prefer]{dragon_prefer()}}, usually continuing from
the student run itself.
}
\examples{
\dontrun{
# Close the loop: sample from the fine-tuned run, let a judge rank, train DPO on the result.
pairs <- dragon_synthesize_pairs(dragon_prompts(sft, "train", n = 200), student = sft,
                                 judge = dragon_judge_anthropic(model = "claude-sonnet-5"))
dpo <- dragon_prefer(pairs, sft, wait = TRUE)
dragon_judge(dpo, against = "base", judge = dragon_judge_anthropic())
}
}
