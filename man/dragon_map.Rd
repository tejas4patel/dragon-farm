% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping.R
\name{dragon_map}
\alias{dragon_map}
\title{Map dataset columns to prompt, response, and system text}
\usage{
dragon_map(dataset, prompt, response, system = NULL)
}
\arguments{
\item{dataset}{A \code{dragon_dataset}.}

\item{prompt}{Column name or template for the user turn.}

\item{response}{Column name or template for the assistant turn.}

\item{system}{Optional column name or template for the system prompt. A
template with no braces and no matching column is used as a constant
system prompt for every row.}
}
\value{
The dataset with the mapping attached.
}
\description{
Each argument is either a column name or a \code{\link[glue:glue]{glue::glue()}} template that
combines several columns, such as \code{"{subject}\\n\\n{body}"}. Templates are
rendered per row when the training files are written.
}
\examples{
ds <- dragon_dataset(dragon_example_data())
ds <- dragon_map(ds, prompt = "{subject}\n\n{body}", response = "reply")
dragon_preview(ds, n = 1)
}
