% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feedback.R
\name{dragon_feedback}
\alias{dragon_feedback}
\title{Training data from chat feedback}
\usage{
dragon_feedback(runs_dir = dragon_runs_dir(), label = NULL)
}
\arguments{
\item{runs_dir}{Runs directory holding \code{feedback/feedback.jsonl}.}

\item{label}{Optional filter: only feedback given to this run id or
backend model.}
}
\value{
A list with \code{records} (a data frame of every feedback event),
\code{sft} (dataset or \code{NULL}), and \code{pairs} (dataset or \code{NULL}).
}
\description{
Ratings and edits made in \code{\link[=dragon_chat]{dragon_chat()}} or the app's Chat panel are
saved under \verb{feedback/} in the runs directory. This turns them into data
for the next stage:
}
\details{
\itemize{
\item \code{sft}: a conversations dataset (\code{\link[=dragon_conversations]{dragon_conversations()}}) of replies
the user liked or edited, each with the turns that led to it. Edited
text replaces the model's reply.
\item \code{pairs}: a preference dataset (\code{\link[=dragon_map_pairs]{dragon_map_pairs()}}) for prompts that
received both a liked (or edited) reply and a disliked one.
}

Both are written as JSONL under \verb{feedback/} so runs trained on them stay
reproducible through \code{\link[=dragon_code]{dragon_code()}}.
}
\examples{
\dontrun{
fb <- dragon_feedback()
fb$records
better <- dragon_train(fb$sft, dpo, wait = TRUE)       # continue from the run people chatted with
dpo2 <- dragon_prefer(fb$pairs, better, wait = TRUE)
}
}
