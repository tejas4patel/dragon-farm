% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{dragon_publish}
\alias{dragon_publish}
\title{Push a run's model to the Hugging Face Hub}
\usage{
dragon_publish(
  run,
  repo,
  what = c("merged", "adapter"),
  private = FALSE,
  commit_message = NULL
)
}
\arguments{
\item{run}{A \code{dragon_run} or run directory.}

\item{repo}{A repo id, \code{"username/name"}.}

\item{what}{What to push. \code{"merged"} folds the adapter into the base
model first (merging it if that has not been done yet), so the repo
loads with plain transformers and needs neither \code{peft} nor
dragonfarm; this is the usual choice for hosted inference. \code{"adapter"}
pushes just the LoRA weights: a small, fast upload, but the base model
and \code{peft} are needed to load it.}

\item{private}{Create the repo as private.}

\item{commit_message}{Defaults to a message naming the run and the stage.}
}
\value{
The repo URL, invisibly.
}
\description{
Uploads a run's model as a repo on the Hugging Face Hub, ready for any
hosted inference: a dedicated Inference Endpoint behind
\code{\link[=dragon_backend_server]{dragon_backend_server()}}, \code{transformers-cli}, or any tool that loads a
model by repo id. The repo is created if it does not exist.
}
\examples{
\dontrun{
dragon_publish(run, "yourname/support-agent-0.5b")
dragon_publish(run, "yourname/support-agent-0.5b-adapter", what = "adapter")
}
}
