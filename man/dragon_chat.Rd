% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat.R
\name{dragon_chat}
\alias{dragon_chat}
\alias{dragon_chat_load}
\title{Talk to a model, with memory of the conversation}
\usage{
dragon_chat(
  x = NULL,
  system = NULL,
  backend = dragon_backend(),
  max_new_tokens = 256,
  temperature = 0.7,
  top_p = 0.9,
  base = FALSE,
  runs_dir = dragon_runs_dir(),
  context_window = NULL
)

dragon_chat_load(path, x = NULL, backend = dragon_backend())
}
\arguments{
\item{x}{What answers: a \code{dragon_run}, adapter or model directory, or
model id. Ignored by server backends, which serve a fixed model.}

\item{system}{Optional system prompt, kept at the top of every turn.}

\item{backend}{Where to run inference. See \link{dragon_backend}.}

\item{max_new_tokens, temperature, top_p}{Generation settings.}

\item{base}{Talk to what the run started from instead of the run.}

\item{runs_dir}{Where feedback from \verb{$rate()} and \verb{$edit()} is recorded
(under \verb{feedback/}). See \code{\link[=dragon_feedback]{dragon_feedback()}}.}

\item{context_window}{Approximate token budget for the conversation
(system prompt plus history), such as a small model's 2K to 8K
context. When the next turn would go over it, \verb{$say()} drops the
oldest user/assistant pair and tries again until it fits, keeping the
system prompt and the most recent turns. \code{NULL} (the default) never
drops turns. The token count is a rough estimate (about 4 characters
per token), not the model's own tokenizer.}

\item{path}{A transcript written by \verb{$save()}.}
}
\value{
A \code{dragon_chat} object with methods:
\verb{$say(text, on_token = NULL)} sends a user turn and returns the reply
(streaming pieces to \code{on_token} when the backend supports it);
\verb{$history()} returns the messages; \verb{$reset()} clears them; \verb{$undo()}
drops the last exchange; \verb{$regenerate()} asks again for the last reply;
\verb{$rate("up")} or \verb{$rate("down")} records a verdict on the last reply
and \verb{$edit(text)} replaces it with a better one, both saved as feedback
that \code{\link[=dragon_feedback]{dragon_feedback()}} turns into training data;
\verb{$context_usage()} reports the estimated tokens used, the window, and
how many turns have been dropped to stay under it (\code{NULL} when
\code{context_window} is not set);
\verb{$save(path)} and \code{dragon_chat_load(path)} write and read a transcript;
\verb{$as_example()} returns the conversation as one training row.
}
\description{
A conversation object that keeps the message history and sends all of it
on every turn, so the model has context. Works with any backend: the
local worker, Ollama, or a remote server. Transcripts use the same
message format as training data, so a good conversation can become an
example.
}
\examples{
\dontrun{
chat <- dragon_chat(run, system = "You are a concise support agent.")
chat$say("My thermostat keeps dropping off Wi-Fi.")
chat$say("I tried that. What else?")   # the model sees the first exchange
chat$history()
chat$save("good-conversation.json")
}
}
