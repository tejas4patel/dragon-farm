% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/judge.R
\name{dragon_llm_anthropic}
\alias{dragon_llm_anthropic}
\alias{dragon_judge_anthropic}
\alias{dragon_llm_ellmer}
\alias{dragon_judge_ellmer}
\title{Language models as functions: the Claude API and ellmer}
\usage{
dragon_llm_anthropic(
  model = "claude-opus-5",
  system = NULL,
  api_key = Sys.getenv("ANTHROPIC_API_KEY"),
  max_tokens = 1024,
  max_active = 4,
  temperature = NULL
)

dragon_judge_anthropic(
  model = "claude-opus-5",
  api_key = Sys.getenv("ANTHROPIC_API_KEY"),
  max_tokens = 1024,
  max_active = 4
)

dragon_llm_ellmer(chat, system = NULL)

dragon_judge_ellmer(chat)
}
\arguments{
\item{model}{Claude model id. The default is the most capable general
model; \code{"claude-sonnet-5"} or \code{"claude-haiku-4-5"} are cheaper choices
for large prompt sets.}

\item{system}{Optional system prompt.}

\item{api_key}{Anthropic API key.}

\item{max_tokens}{Reply length cap.}

\item{max_active}{How many requests to run at once.}

\item{temperature}{Sampling temperature, or \code{NULL} for the API default.}

\item{chat}{An \code{ellmer} chat object, for example \code{ellmer::chat_anthropic()}.}
}
\value{
A function suitable for the \code{judge}, \code{teacher}, or \code{student}
arguments of \code{\link[=dragon_judge]{dragon_judge()}}, \code{\link[=dragon_synthesize]{dragon_synthesize()}}, and
\code{\link[=dragon_synthesize_pairs]{dragon_synthesize_pairs()}}.
}
\description{
Judges, teachers, and students in dragonfarm are plain functions from a
character vector of prompts to a character vector of replies. These
helpers build such functions.
}
\details{
\code{dragon_llm_anthropic()} calls the Claude API directly over HTTP with
refusal fallbacks enabled, reading the key from \code{ANTHROPIC_API_KEY}.
\code{dragon_llm_ellmer()} wraps any \code{ellmer} chat, so every provider ellmer
supports works; each prompt gets a fresh copy of the chat so no history
leaks between questions. The \verb{dragon_judge_*()} variants are the same
with a system prompt that asks for JSON-only answers, which
\code{\link[=dragon_judge]{dragon_judge()}} and \code{\link[=dragon_synthesize_pairs]{dragon_synthesize_pairs()}} need.
}
\examples{
\dontrun{
teacher <- dragon_llm_anthropic(system = "You are a concise support agent.")
teacher(c("My thermostat drops off Wi-Fi.", "Invoice total looks wrong."))
}
}
