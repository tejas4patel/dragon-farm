% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend.R
\name{dragon_backend}
\alias{dragon_backend}
\alias{dragon_backend_local}
\alias{dragon_backend_server}
\alias{dragon_backend_ollama}
\title{Inference backends}
\usage{
dragon_backend_local(keep_loaded = TRUE, device = "auto", dtype = "auto")

dragon_backend_server(url, model, api_key = NULL, headers = NULL)

dragon_backend_ollama(model, url = "http://localhost:11434")

dragon_backend()
}
\arguments{
\item{keep_loaded}{Keep the worker and its models alive between calls.}

\item{device, dtype}{Device and precision for the local worker. See
\code{\link[=dragon_hardware]{dragon_hardware()}}.}

\item{url}{Base URL of the server, ending in \verb{/v1}.}

\item{model}{Model name as the server knows it.}

\item{api_key}{Bearer token, if the server needs one. Defaults to
\code{DRAGONFARM_API_KEY}, then \code{OPENAI_API_KEY}.}

\item{headers}{Extra HTTP headers as a named character vector.}
}
\value{
A \code{dragon_backend} object.
}
\description{
Every function that generates text, \code{\link[=dragon_generate]{dragon_generate()}}, \code{\link[=dragon_chat]{dragon_chat()}},
the judge, teacher, and synthesis helpers, takes a \code{backend}. The default
is the local worker; set \code{options(dragonfarm.backend = ...)} to change it
for a session.
}
\details{
\itemize{
\item \code{dragon_backend_local()}: a Python worker on this machine that keeps
the last two models loaded, so repeated calls pay the model load once.
\code{keep_loaded = FALSE} stops it after every call.
\item \code{dragon_backend_server()}: any server that speaks the OpenAI chat
completions protocol. \code{url} is the base that ends in \verb{/v1}. The model
must already be available on that server; see \code{\link[=dragon_serve_ollama]{dragon_serve_ollama()}}
for the local case.
\item \code{dragon_backend_ollama()}: a server backend for a model registered
with Ollama on this machine.
\item \code{dragon_backend()}: the session default.
}
}
\examples{
\dontrun{
options(dragonfarm.backend = dragon_backend_ollama("support-0.5b"))
dragon_generate(run, "My thermostat keeps dropping off Wi-Fi.")

vllm <- dragon_backend_server("https://my-pod.example.com/v1", model = "tejas/support-0.5b")
dragon_generate(run, "Hello", backend = vllm)
}
}
