% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth.R
\name{dragon_synthesize}
\alias{dragon_synthesize}
\title{Write fine-tuning data with a teacher model}
\usage{
dragon_synthesize(
  prompts,
  teacher,
  system = NULL,
  max_new_tokens = 512,
  temperature = 0.7,
  file = NULL,
  runs_dir = dragon_runs_dir(),
  name = "synthetic"
)
}
\arguments{
\item{prompts}{A character vector of prompts, or a mapped \code{dragon_dataset}
whose prompt template is rendered for every row. See \code{\link[=dragon_prompts]{dragon_prompts()}}
for prompts from an existing run.}

\item{teacher}{The model that writes the replies: \code{\link[=dragon_llm_anthropic]{dragon_llm_anthropic()}},
an \code{ellmer} chat, a model id or directory, a finished run, or any
function from prompts to replies.}

\item{system}{System prompt for the teacher. Also stored with every row
so the student trains with the same instruction.}

\item{max_new_tokens}{Reply length cap for local teachers.}

\item{temperature}{Sampling temperature for local teachers.}

\item{file}{Where to write the JSONL. Defaults to a timestamped file under
\verb{synth/} in \code{runs_dir}.}

\item{runs_dir}{Parent directory for the default \code{file}.}

\item{name}{Label used in the default file name.}
}
\value{
A \code{dragon_dataset} mapped with prompt and response columns (and
system, when given). The file path is its \code{source}, so \code{\link[=dragon_code]{dragon_code()}}
reproduces runs trained on it.
}
\description{
Sends each prompt to a stronger model and keeps its replies as the
responses to train on. This is the fastest way to get good training data
for a small model: a few hundred prompts from your domain, answered the
way you want them answered. The result is saved as JSONL and returned as a
mapped dataset ready for \code{\link[=dragon_train]{dragon_train()}}.
}
\examples{
\dontrun{
tickets <- dragon_dataset("tickets.csv") |>
  dragon_map(prompt = "{subject}\n\n{body}", response = "reply")
persona <- "You are a concise, warm support agent for a smart-home company."
teacher <- dragon_llm_anthropic(system = persona)
synth <- dragon_synthesize(tickets, teacher, system = persona)
run <- dragon_train(synth, "Qwen/Qwen2.5-0.5B-Instruct", wait = TRUE)
}
}
