% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/remote.R
\name{dragon_remote}
\alias{dragon_remote}
\title{Open a cloud GPU provider for a bundled run}
\usage{
dragon_remote(
  run,
  provider = c("colab", "kaggle", "lightning", "runpod"),
  open = interactive()
)
}
\arguments{
\item{run}{A \code{dragon_run} or run directory.}

\item{provider}{One of \code{"colab"}, \code{"kaggle"}, \code{"lightning"}, \code{"runpod"}.
See \code{\link[=dragon_remote_providers]{dragon_remote_providers()}}.}

\item{open}{Open the provider link in the browser.}
}
\value{
Invisibly, a list with \code{provider}, \code{url}, \code{bundle} (path to the
zip), and \code{steps} (a character vector).
}
\description{
Prints the steps for running a bundled run on the chosen provider and, by
default in an interactive session, opens the provider in the browser with
the dragon-farm notebook loaded. Runs that were not bundled yet are bundled
first, so a run that failed locally for lack of memory can be sent to the
cloud as is.
}
\examples{
\dontrun{
dragon_remote(run, "kaggle")
}
}
