% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reinforce.R
\name{dragon_map_prompts}
\alias{dragon_map_prompts}
\title{Map columns for reinforcement learning}
\usage{
dragon_map_prompts(dataset, prompt, reference = NULL, system = NULL)
}
\arguments{
\item{dataset}{A \code{dragon_dataset}.}

\item{prompt}{Column name or template for the user turn.}

\item{reference}{Optional column name or template for the reference
answer.}

\item{system}{Optional column name or template for the system prompt. A
template with no braces and no matching column is used as a constant
system prompt for every row.}
}
\value{
The dataset with the mapping attached.
}
\description{
For \code{\link[=dragon_reinforce]{dragon_reinforce()}}. Each row is a prompt the model will practise
on, with an optional reference answer that reward functions such as
\code{"exact"} and \code{"numeric"} compare against. Every other column travels
along as \code{fields}, available to custom reward functions.
}
\examples{
df <- data.frame(question = c("12 * 12?", "Capital of Peru?"), answer = c("144", "Lima"))
ds <- dragon_map_prompts(dragon_dataset(df), prompt = "question", reference = "answer")
dragon_preview(ds, n = 1)
}
