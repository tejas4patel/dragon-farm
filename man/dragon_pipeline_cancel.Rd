% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{dragon_pipeline_cancel}
\alias{dragon_pipeline_cancel}
\title{Cancel a pipeline}
\usage{
dragon_pipeline_cancel(
  x,
  runs_dir = dragon_runs_dir(),
  wait = TRUE,
  timeout = 120
)
}
\arguments{
\item{x}{A \code{dragon_pipeline} handle or a pipeline id.}

\item{runs_dir}{Where the pipeline record lives, when \code{x} is an id.}

\item{wait}{Wait for the pipeline to stop.}

\item{timeout}{Seconds to wait before killing the pipeline process.}
}
\value{
The pipeline status, invisibly.
}
\description{
Writes a cancel request next to the pipeline record. The runner checks it
between steps and stops there. A training step that is under way is asked
to stop as well, the way \code{\link[=dragon_cancel]{dragon_cancel()}} does, so it saves a checkpoint
first; a judging, synthesis, or merge step finishes before the pipeline
stops. With \code{wait = TRUE} the call returns once the pipeline has stopped,
killing the pipeline process if it is still going after \code{timeout} seconds.
}
