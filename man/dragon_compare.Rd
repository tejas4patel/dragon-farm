% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{dragon_compare}
\alias{dragon_compare}
\title{Compare runs side by side}
\usage{
dragon_compare(..., runs_dir = dragon_runs_dir())
}
\arguments{
\item{...}{Runs, run directories, a single \code{dragon_pipeline} (its runs
are compared), or nothing to list every run in \code{runs_dir}.}

\item{runs_dir}{Directory scanned when no runs are given.}
}
\value{
A data frame of class \code{dragon_comparison}.
}
\description{
One row per run with its stage, what it started from, and every number
the package knows about it: held-out loss, perplexity, preference
accuracy, task metrics from \code{\link[=dragon_evaluate]{dragon_evaluate()}}, and the latest judge
result from \code{\link[=dragon_judge]{dragon_judge()}}. Runs that lack a measurement show \code{NA}.
}
\examples{
\dontrun{
dragon_compare()                 # everything in the runs directory
dragon_compare(sft, dpo)         # two specific runs
}
}
